# Third-party notices

Runtime dependencies are declared in `package.json` and pinned in `package-lock.json`. Their licenses and notices remain those distributed by npm. Any future vendored provider code must include its upstream commit and license here before release.
