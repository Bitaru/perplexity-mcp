import { ProviderError } from "./errors";
export type SearchResult={answer:string;sources:{url:string;title?:string}[]};
export function parseProviderJson(value:unknown):SearchResult { if(!value||typeof value!=="object")throw new ProviderError("PERPLEXITY_PROTOCOL_ERROR","malformed payload"); const v=value as {answer?:unknown;sources?:unknown}; if(typeof v.answer!=="string"||!v.answer.trim())throw new ProviderError("PERPLEXITY_PROTOCOL_ERROR","missing answer"); if(v.sources!==undefined&&!Array.isArray(v.sources))throw new ProviderError("PERPLEXITY_PROTOCOL_ERROR","malformed sources"); const sources=(v.sources??[]).map(x=>{if(!x||typeof x!=="object")throw new ProviderError("PERPLEXITY_PROTOCOL_ERROR","malformed source");const s=x as {url?:unknown;title?:unknown};if(typeof s.url!=="string"||!/^https?:\/\//.test(s.url))throw new ProviderError("PERPLEXITY_PROTOCOL_ERROR","malformed source");return typeof s.title==="string"?{url:s.url,title:s.title}:{url:s.url};});return {answer:v.answer,sources}; }
